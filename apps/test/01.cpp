#include <iostream>
using namespace std;

int find(vector<int> &nums)
{
    vector(begin, end);
    push_back
    at()
    front()
    back()
    empty()

    sort(obj.begin(), obj.end());

    recerse(obj.begin(), obj.end());

    hash_map<int, string> mymqy;
    mymap[] = ""
    my.find() != mymap.end()
    ::iterator itend;
    while(it != end)
        it++;
    
    list<int> a();

    list.push_front();
    list.push_bakc();
    for(it = lst.begin;)

}






int main()
{
    char a[20];
    cin.getline(a, 10);
    cout<<a<<endl;

    vector<int> arr(n, 1);
    
    while(scanf("%d %d", a, b ) != EOF)
    {
        printf("%d", a + b);
    }
    char a[10];
    cin.get(a, );
    stack.pop()
    stack.push_back();
    stack.init()

    return 0;
}


make([]type, len)
make(map[string]int)

for k ,v := range m{

}